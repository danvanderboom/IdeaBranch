<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:IdeaBranch.App.ViewModels.Analytics"
             xmlns:controls="clr-namespace:IdeaBranch.App.Controls"
             x:Class="IdeaBranch.App.Views.TimelinePage"
             x:DataType="vm:TimelineViewModel"
             Title="Timeline Analytics"
             AutomationId="TimelinePage">
    <Grid RowDefinitions="Auto,*" RowSpacing="10" Padding="10">
        <!-- Filter Panel -->
        <Border Grid.Row="0" BackgroundColor="{StaticResource Secondary}" Padding="10" StrokeThickness="1" Stroke="{StaticResource Gray500}">
            <VerticalStackLayout Spacing="10">
                <Label Text="Filters" FontSize="18" FontAttributes="Bold" />
                
                <!-- Source Types -->
                <VerticalStackLayout Spacing="5">
                    <Label Text="Source Types:" FontSize="14" FontAttributes="Bold" />
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="{Binding IncludeTopics}" />
                        <Label Text="Topics" VerticalOptions="Center" />
                        <CheckBox IsChecked="{Binding IncludeAnnotations}" />
                        <Label Text="Annotations" VerticalOptions="Center" />
                        <CheckBox IsChecked="{Binding IncludeConversations}" />
                        <Label Text="Conversations" VerticalOptions="Center" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <!-- Event Types -->
                <VerticalStackLayout Spacing="5">
                    <Label Text="Event Types:" FontSize="14" FontAttributes="Bold" />
                    <HorizontalStackLayout Spacing="10">
                        <CheckBox IsChecked="{Binding ShowCreated}" />
                        <Label Text="Created" VerticalOptions="Center" />
                        <CheckBox IsChecked="{Binding ShowUpdated}" />
                        <Label Text="Updated" VerticalOptions="Center" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <!-- Search -->
                <VerticalStackLayout Spacing="5">
                    <Label Text="Search:" FontSize="14" FontAttributes="Bold" />
                    <Entry Text="{Binding SearchQuery}" 
                            Placeholder="Search events (min 2 characters)" />
                </VerticalStackLayout>

                <!-- Tags -->
                <VerticalStackLayout Spacing="5">
                    <Label Text="Tags:" FontSize="14" FontAttributes="Bold" />
                    <Label Text="{Binding SelectedTagSelections.Count, StringFormat='Selected: {0} tag(s)'}" 
                           FontSize="12" 
                           TextColor="Gray" />
                    <Button Text="Select Tags..." 
                            Clicked="OnTagPickerClicked" />
                    <!-- TODO: Replace with TagPickerPopup when enhanced -->
                </VerticalStackLayout>

                <!-- Date Range -->
                <VerticalStackLayout Spacing="5">
                    <Label Text="Date Range:" FontSize="14" FontAttributes="Bold" />
                    <Label Text="{Binding ActiveDatePreset}" 
                           FontSize="12" 
                           TextColor="Blue" 
                           IsVisible="{Binding ActiveDatePreset, Converter={StaticResource IsNotNullConverter}}" />
                    
                    <!-- Quick Presets -->
                    <HorizontalStackLayout Spacing="10">
                        <Button Text="Last 7 days" 
                                Command="{Binding ApplyLast7DaysCommand}" 
                                FontSize="12" />
                        <Button Text="This month" 
                                Command="{Binding ApplyThisMonthCommand}" 
                                FontSize="12" />
                        <Button Text="This year" 
                                Command="{Binding ApplyThisYearCommand}" 
                                FontSize="12" />
                    </HorizontalStackLayout>
                    
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <VerticalStackLayout Grid.Column="0" Spacing="5">
                            <Label Text="Start Date:" />
                            <DatePicker Date="{Binding StartDate}" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1" Spacing="5">
                            <Label Text="End Date:" />
                            <DatePicker Date="{Binding EndDate}" />
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>

                <!-- Generate Button -->
                <Button Text="Generate Timeline" 
                        Command="{Binding GenerateCommand}" 
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}" />
                
                <!-- Error Message -->
                <Label Text="{Binding ErrorMessage}" 
                       IsVisible="{Binding HasError}" 
                       TextColor="Red" />
            </VerticalStackLayout>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <VerticalStackLayout Spacing="10" Padding="10">
                <!-- Loading Indicator -->
                <ActivityIndicator IsRunning="{Binding IsLoading}" 
                                  IsVisible="{Binding IsLoading}" 
                                  HeightRequest="50" />

                        <!-- Timeline Display -->
                        <Border IsVisible="{Binding HasData}" 
                               BackgroundColor="White" 
                               Padding="10" 
                               StrokeThickness="1" 
                               Stroke="{StaticResource Gray500}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="{Binding EventCount, StringFormat='Total Events: {0}'}" 
                               FontSize="16" 
                               FontAttributes="Bold" />
                        
                        <!-- Interactive SkiaSharp Timeline -->
                        <controls:SkiaTimelineView x:Name="TimelineView"
                                                  Events="{Binding TimelineEventViews}"
                                                  HeightRequest="400" />

                        <!-- Export Buttons -->
                        <HorizontalStackLayout Spacing="10">
                            <Button Text="Export JSON" Command="{Binding ExportJsonCommand}" />
                            <Button Text="Export CSV" Command="{Binding ExportCsvCommand}" />
                            <Button Text="Export PNG" Command="{Binding ExportPngCommand}" />
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <!-- Empty State -->
                <Label Text="No timeline data. Click 'Generate Timeline' to create one." 
                       IsVisible="{Binding HasData, Converter={StaticResource InvertedBoolConverter}}" 
                       HorizontalOptions="Center" 
                       VerticalOptions="Center" 
                       TextColor="Gray" />
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>
